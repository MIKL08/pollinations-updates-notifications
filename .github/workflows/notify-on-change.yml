name: Pollinations Models Monitor

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

permissions:
  contents: read

env:
  GLOT_TOKEN: ${{ secrets.GLOT_TOKEN }}
  GLOT_ID_TEXT: ${{ secrets.GLOT_ID }}
  GLOT_ID_IMAGE: ${{ secrets.GLOT_ID_IMAGE }}
  GLOT_ID_TEXT_POLLENS: ${{ secrets.GLOT_ID_TEXT_POLLENS }}
  GLOT_ID_IMAGE_POLLENS: ${{ secrets.GLOT_ID_IMAGE_POLLENS }}
  TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
  TELEGRAM_TO_TEXT: ${{ secrets.TELEGRAM_TO }}
  TELEGRAM_TO_IMAGE: ${{ secrets.TELEGRAM_TO }}
  TELEGRAM_TO_ERROR: ${{ secrets.TELEGRAM_TO_ERROR }}
  TELEGRAM_TO_BETA: ${{ secrets.TELEGRAM_TO }}

jobs:
  check-text-models:
    runs-on: ubuntu-latest
    steps:
      - name: Get current date
        id: date
        run: echo "date=$(date +'%Y-%m-%d')" >> $GITHUB_OUTPUT

      - name: Restore text models from glot.io
        id: restore
        continue-on-error: true
        run: |
          RESPONSE=$(curl -s -X GET "https://glot.io/api/snippets/${GLOT_ID_TEXT}" \
            -H "Authorization: Token ${GLOT_TOKEN}")
          if [ "$(echo "$RESPONSE" | jq -r '.files[0].content')" != "null" ]; then
            echo "$RESPONSE" | jq -r '.files[0].content' > pollinations_text_models.json
            echo "restored=true" >> $GITHUB_OUTPUT
          else
            echo "restored=false" >> $GITHUB_OUTPUT
          fi

      - name: Fetch text models with retries
        id: fetch
        continue-on-error: true
        run: |
          set -euo pipefail
          for i in {1..3}; do
            if curl -sS --fail -A "github-actions-bot" "https://text.pollinations.ai/models" -o /tmp/text_models.json; then
              echo "status=success" >> $GITHUB_OUTPUT
              exit 0
            fi
            echo "Attempt $i/3 failed"
            sleep 10
          done
          echo "::error::POLLINATIONS TEXT API IS DOWN - Failed after 3 attempts"
          echo "status=failure" >> $GITHUB_OUTPUT
          exit 0

      - name: Compare text models
        if: steps.fetch.outputs.status == 'success'
        id: compare
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json, sys
          from pathlib import Path

          def normalize_model(m):
              m = dict(m)
              for key in ("input_modalities", "output_modalities", "voices", "tools"):
                  if key in m and isinstance(m[key], list):
                      m[key] = sorted(set(m[key]))
              return m

          tmp = Path("/tmp/text_models.json")
          try:
              data = json.loads(tmp.read_text(encoding="utf-8"))
          except Exception as e:
              print(f"::error::Failed to parse JSON: {e}")
              sys.exit(1)

          normalized = [normalize_model(m) for m in data]
          normalized.sort(key=lambda x: str(x.get("name","")))

          curr_txt = json.dumps(normalized, ensure_ascii=False, indent=2, sort_keys=True) + "\n"

          prev_path = Path("pollinations_text_models.json")
          lines = []

          if not prev_path.exists():
              prev_path.write_text(curr_txt, encoding="utf-8")
              Path("/tmp/changed.flag").write_text("false", encoding="utf-8")
              Path("/tmp/message.txt").write_text("First snapshot initialized. No notification sent.\n", encoding="utf-8")
              sys.exit(0)

          prev = json.loads(prev_path.read_text(encoding="utf-8"))

          def to_map(items):
              return {str(i.get("name","")): i for i in items if i.get("name")}

          prev_map = to_map(prev)
          curr_map = to_map(normalized)

          added = sorted(set(curr_map) - set(prev_map))
          removed = sorted(set(prev_map) - set(curr_map))
          common = sorted(set(prev_map) & set(curr_map))

          modified = {}

          def diff_dicts(o,n):
              d={}
              for k in sorted(set(o)|set(n)):
                  ov=o.get(k); nv=n.get(k)
                  if ov!=nv:
                      if isinstance(ov, list) and isinstance(nv, list):
                          if set(ov) != set(nv):
                              d[k] = {"old":ov, "new":nv}
                      else:
                          d[k] = {"old":ov, "new":nv}
              return d

          for name in common:
              d = diff_dicts(prev_map[name], curr_map[name])
              if d:
                  modified[name]=d

          has_changes = bool(added or removed or modified)

          if added:
              lines.append("\n<b>+ Added models:</b>")
              for n in added:
                  m=curr_map[n]
                  lines.append(f"  - <code>{n}</code> (Description: {m.get('description','?')}, Tier: {m.get('tier','?')})")

          if removed:
              lines.append("\n<b>- Deleted models:</b>")
              for n in removed:
                  m=prev_map[n]
                  lines.append(f"  - <code>{n}</code> (Description: {m.get('description','?')}, Tier: {m.get('tier','?')})")

          if modified:
              lines.append("\n<b>&gt; Updated models:</b>")
              for n in sorted(modified):
                  lines.append(f"  - <code>{n}</code>:")
                  for k,ch in modified[n].items():
                      lines.append(f"      • {k}: {ch['old']} → {ch['new']}")

          if not has_changes:
              lines.append("No changes found.")

          Path("/tmp/message.txt").write_text("\n".join(lines) + "\n", encoding="utf-8")
          Path("/tmp/changed.flag").write_text("true" if has_changes else "false", encoding="utf-8")

          if has_changes:
              prev_path.write_text(curr_txt, encoding="utf-8")
          PY

          CHANGED=$(cat /tmp/changed.flag 2>/dev/null || echo false)
          if [ "$CHANGED" = "true" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

          {
            echo 'message<<EOF'
            cat /tmp/message.txt
            echo EOF
          } >> "$GITHUB_OUTPUT"

      - name: Notify text model changes
        if: steps.compare.outputs.changed == 'true'
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO_TEXT }}
          token: ${{ env.TELEGRAM_TOKEN }}
          message: |
            <b>💬 Pollinations AI Text Models Update:</b>
            ${{ steps.compare.outputs.message }}
          format: html

      - name: Alert text API failure
        if: steps.fetch.outputs.status == 'failure'
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO_ERROR }}
          token: ${{ env.TELEGRAM_TOKEN }}
          message: |
            ‼️<i>Maybe</i> <b>Pollinations AI Text API is down</b>‼️
            Failed 3 times to fetch models.
          format: html

  check-image-models:
    runs-on: ubuntu-latest
    steps:
      - name: Restore image models from glot.io
        id: restore
        continue-on-error: true
        run: |
          RESPONSE=$(curl -s -X GET "https://glot.io/api/snippets/${GLOT_ID_IMAGE}" \
            -H "Authorization: Token ${GLOT_TOKEN}")
          if [ "$(echo "$RESPONSE" | jq -r '.files[0].content')" != "null" ]; then
            echo "$RESPONSE" | jq -r '.files[0].content' > pollinations_image_models.json
            echo "restored=true" >> $GITHUB_OUTPUT
          else
            echo "restored=false" >> $GITHUB_OUTPUT
          fi

      - name: Fetch image models with retries
        id: fetch
        continue-on-error: true
        run: |
          set -euo pipefail
          for i in {1..3}; do
            if curl -sS --fail -A "github-actions-bot" "https://image.pollinations.ai/models" -o /tmp/image_models.json; then
              echo "status=success" >> $GITHUB_OUTPUT
              exit 0
            fi
            echo "Attempt $i/3 failed"
            sleep 10
          done
          echo "::error::IMAGE MODELS SOURCE IS DOWN - Failed after 3 attempts"
          echo "status=failure" >> $GITHUB_OUTPUT
          exit 0

      - name: Compare image models
        if: steps.fetch.outputs.status == 'success'
        id: compare
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json, sys
          from pathlib import Path

          tmp = Path("/tmp/image_models.json")
          try:
              curr_list = json.loads(tmp.read_text(encoding="utf-8"))
              if not isinstance(curr_list, list):
                  raise ValueError("Expected a JSON array")
              curr_set = set(str(x) for x in curr_list if x)
          except Exception as e:
              print(f"::error::Failed to parse image models JSON: {e}")
              sys.exit(1)

          prev_path = Path("pollinations_image_models.json")
          lines = []

          if not prev_path.exists():
              prev_path.write_text(json.dumps(sorted(curr_set), indent=2) + "\n", encoding="utf-8")
              Path("/tmp/changed.flag").write_text("false", encoding="utf-8")
              Path("/tmp/message.txt").write_text("First image models snapshot initialized. No notification sent.\n", encoding="utf-8")
              sys.exit(0)

          try:
              prev_list = json.loads(prev_path.read_text(encoding="utf-8"))
              prev_set = set(str(x) for x in prev_list if x)
          except Exception as e:
              print(f"::error::Failed to parse previous image models: {e}")
              sys.exit(1)

          added = sorted(curr_set - prev_set)
          removed = sorted(prev_set - curr_set)
          has_changes = bool(added or removed)

          if added:
              lines.append("\n<b>+ Added image models:</b>")
              for n in added:
                  lines.append(f"  - <code>{n}</code>")

          if removed:
              lines.append("\n<b>- Removed image models:</b>")
              for n in removed:
                  lines.append(f"  - <code>{n}</code>")

          if not has_changes:
              lines.append("No changes found.")

          Path("/tmp/message.txt").write_text("\n".join(lines) + "\n", encoding="utf-8")
          Path("/tmp/changed.flag").write_text("true" if has_changes else "false", encoding="utf-8")

          if has_changes:
              prev_path.write_text(json.dumps(sorted(curr_set), indent=2) + "\n", encoding="utf-8")
          PY

          CHANGED=$(cat /tmp/changed.flag 2>/dev/null || echo false)
          if [ "$CHANGED" = "true" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

          {
            echo 'message<<EOF'
            cat /tmp/message.txt
            echo EOF
          } >> "$GITHUB_OUTPUT"

      - name: Notify image model changes
        if: steps.compare.outputs.changed == 'true'
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO_IMAGE }}
          token: ${{ env.TELEGRAM_TOKEN }}
          message: |
            <b>🎨 Pollinations AI Image Models Update:</b>
            ${{ steps.compare.outputs.message }}
          format: html

      - name: Alert image source failure
        if: steps.fetch.outputs.status == 'failure'
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO_ERROR }}
          token: ${{ env.TELEGRAM_TOKEN }}
          message: |
            ‼️<i>Maybe</i> <b>Pollinations AI Image Models source is down</b>‼️
            Failed 3 times to fetch image model list.
          format: html

  check-text-models-pollens:
    runs-on: ubuntu-latest
    steps:
      - name: Restore text models from glot.io
        id: restore
        continue-on-error: true
        run: |
          RESPONSE=$(curl -s -X GET "https://glot.io/api/snippets/${GLOT_ID_TEXT_POLLENS}" \
            -H "Authorization: Token ${GLOT_TOKEN}")
          if [ "$(echo "$RESPONSE" | jq -r '.files[0].content')" != "null" ]; then
            echo "$RESPONSE" | jq -r '.files[0].content' > pollens_text_models.json
            echo "restored=true" >> $GITHUB_OUTPUT
          else
            echo "restored=false" >> $GITHUB_OUTPUT
          fi

      - name: Fetch text models with retries
        id: fetch
        continue-on-error: true
        run: |
          set -euo pipefail
          for i in {1..3}; do
            if curl -sS --fail -A "github-actions-bot" "https://enter.pollinations.ai/api/generate/v1/models" -o /tmp/text_models.json; then
              echo "status=success" >> $GITHUB_OUTPUT
              exit 0
            fi
            echo "Attempt $i/3 failed"
            sleep 10
          done
          echo "::error::POLLENS TEXT API IS DOWN - Failed after 3 attempts"
          echo "status=failure" >> $GITHUB_OUTPUT
          exit 0

      - name: Compare text models
        if: steps.fetch.outputs.status == 'success'
        id: compare
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json, sys
          from pathlib import Path

          tmp = Path("/tmp/text_models.json")
          try:
              data = json.loads(tmp.read_text(encoding="utf-8"))
          except Exception as e:
              print(f"::error::Failed to parse JSON: {e}")
              sys.exit(1)

          models = data.get("data", []) if isinstance(data, dict) else data
          normalized = [dict(m) for m in models]
          normalized.sort(key=lambda x: str(x.get("id","")))

          curr_txt = json.dumps(normalized, ensure_ascii=False, indent=2, sort_keys=True) + "\n"

          prev_path = Path("pollens_text_models.json")
          lines = []

          if not prev_path.exists():
              prev_path.write_text(curr_txt, encoding="utf-8")
              Path("/tmp/changed.flag").write_text("false", encoding="utf-8")
              Path("/tmp/message.txt").write_text("First snapshot initialized. No notification sent.\n", encoding="utf-8")
              sys.exit(0)

          prev = json.loads(prev_path.read_text(encoding="utf-8"))

          def to_map(items):
              return {str(i.get("id","")): i for i in items if i.get("id")}

          prev_map = to_map(prev)
          curr_map = to_map(normalized)

          added = sorted(set(curr_map) - set(prev_map))
          removed = sorted(set(prev_map) - set(curr_map))
          common = sorted(set(prev_map) & set(curr_map))

          modified = {}

          def diff_dicts(o,n):
              d={}
              for k in sorted(set(o)|set(n)):
                  if k == "created":
                      continue
                  ov=o.get(k); nv=n.get(k)
                  if ov!=nv:
                      d[k] = {"old":ov, "new":nv}
              return d

          for name in common:
              d = diff_dicts(prev_map[name], curr_map[name])
              if d:
                  modified[name]=d

          has_changes = bool(added or removed or modified)

          if added:
              lines.append("\n<b>+ Added models:</b>")
              for n in added:
                  m=curr_map[n]
                  lines.append(f"  - <code>{n}</code> (Owned by: {m.get('owned_by','?')})")

          if removed:
              lines.append("\n<b>- Deleted models:</b>")
              for n in removed:
                  m=prev_map[n]
                  lines.append(f"  - <code>{n}</code> (Owned by: {m.get('owned_by','?')})")

          if modified:
              lines.append("\n<b>> Updated models:</b>")
              for n in sorted(modified):
                  lines.append(f"  - <code>{n}</code>:")
                  for k,ch in modified[n].items():
                      lines.append(f"      • {k}: {ch['old']} → {ch['new']}")

          if not has_changes:
              lines.append("No changes found.")

          Path("/tmp/message.txt").write_text("\n".join(lines) + "\n", encoding="utf-8")
          Path("/tmp/changed.flag").write_text("true" if has_changes else "false", encoding="utf-8")

          if has_changes:
              prev_path.write_text(curr_txt, encoding="utf-8")
          PY

          CHANGED=$(cat /tmp/changed.flag 2>/dev/null || echo false)
          if [ "$CHANGED" = "true" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

          {
            echo 'message<<EOF'
            cat /tmp/message.txt
            echo EOF
          } >> "$GITHUB_OUTPUT"

      - name: Notify text model changes
        if: steps.compare.outputs.changed == 'true'
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO_BETA }}
          token: ${{ env.TELEGRAM_TOKEN }}
          message: |
            <b>🛠️ Pollinations AI (<a href="https://enter.pollinations.ai/">Beta with pollens system</a>) Text Models Update:</b>
            ${{ steps.compare.outputs.message }}
          format: html
          disable_web_page_preview: true

      - name: Alert text API failure
        if: steps.fetch.outputs.status == 'failure'
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO_ERROR }}
          token: ${{ env.TELEGRAM_TOKEN }}
          message: |
            ‼️<i>Maybe</i> <b>Pollens AI Text API is down</b>‼️
            Failed 3 times to fetch models.
          format: html

  check-image-models-pollens:
    runs-on: ubuntu-latest
    steps:
      - name: Restore image models from glot.io
        id: restore
        continue-on-error: true
        run: |
          RESPONSE=$(curl -s -X GET "https://glot.io/api/snippets/${GLOT_ID_IMAGE_POLLENS}" \
            -H "Authorization: Token ${GLOT_TOKEN}")
          if [ "$(echo "$RESPONSE" | jq -r '.files[0].content')" != "null" ]; then
            echo "$RESPONSE" | jq -r '.files[0].content' > pollens_image_models.json
            echo "restored=true" >> $GITHUB_OUTPUT
          else
            echo "restored=false" >> $GITHUB_OUTPUT
          fi

      - name: Fetch image models with retries
        id: fetch
        continue-on-error: true
        run: |
          set -euo pipefail
          for i in {1..3}; do
            if curl -sS --fail -A "github-actions-bot" "https://enter.pollinations.ai/api/generate/image/models" -o /tmp/image_models.json; then
              echo "status=success" >> $GITHUB_OUTPUT
              exit 0
            fi
            echo "Attempt $i/3 failed"
            sleep 10
          done
          echo "::error::POLLENS IMAGE MODELS SOURCE IS DOWN - Failed after 3 attempts"
          echo "status=failure" >> $GITHUB_OUTPUT
          exit 0

      - name: Compare image models
        if: steps.fetch.outputs.status == 'success'
        id: compare
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json, sys
          from pathlib import Path

          tmp = Path("/tmp/image_models.json")
          try:
              curr_list = json.loads(tmp.read_text(encoding="utf-8"))
              if not isinstance(curr_list, list):
                  raise ValueError("Expected a JSON array")
              curr_set = set(str(x) for x in curr_list if x)
          except Exception as e:
              print(f"::error::Failed to parse image models JSON: {e}")
              sys.exit(1)

          prev_path = Path("pollens_image_models.json")
          lines = []

          if not prev_path.exists():
              prev_path.write_text(json.dumps(sorted(curr_set), indent=2) + "\n", encoding="utf-8")
              Path("/tmp/changed.flag").write_text("false", encoding="utf-8")
              Path("/tmp/message.txt").write_text("First image models snapshot initialized. No notification sent.\n", encoding="utf-8")
              sys.exit(0)

          try:
              prev_list = json.loads(prev_path.read_text(encoding="utf-8"))
              prev_set = set(str(x) for x in prev_list if x)
          except Exception as e:
              print(f"::error::Failed to parse previous image models: {e}")
              sys.exit(1)

          added = sorted(curr_set - prev_set)
          removed = sorted(prev_set - curr_set)
          has_changes = bool(added or removed)

          if added:
              lines.append("\n<b>+ Added image models:</b>")
              for n in added:
                  lines.append(f"  - <code>{n}</code>")

          if removed:
              lines.append("\n<b>- Removed image models:</b>")
              for n in removed:
                  lines.append(f"  - <code>{n}</code>")

          if not has_changes:
              lines.append("No changes found.")

          Path("/tmp/message.txt").write_text("\n".join(lines) + "\n", encoding="utf-8")
          Path("/tmp/changed.flag").write_text("true" if has_changes else "false", encoding="utf-8")

          if has_changes:
              prev_path.write_text(json.dumps(sorted(curr_set), indent=2) + "\n", encoding="utf-8")
          PY

          CHANGED=$(cat /tmp/changed.flag 2>/dev/null || echo false)
          if [ "$CHANGED" = "true" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

          {
            echo 'message<<EOF'
            cat /tmp/message.txt
            echo EOF
          } >> "$GITHUB_OUTPUT"

      - name: Notify image model changes
        if: steps.compare.outputs.changed == 'true'
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO_BETA }}
          token: ${{ env.TELEGRAM_TOKEN }}
          message: |
            <b>⚙️ Pollinations AI (<a href="https://enter.pollinations.ai/">Beta with pollens system</a>) Image Models Update:</b>
            ${{ steps.compare.outputs.message }}
          format: html
          disable_web_page_preview: true

      - name: Alert image source failure
        if: steps.fetch.outputs.status == 'failure'
        uses: appleboy/telegram-action@master
        with:
          to: ${{ env.TELEGRAM_TO_ERROR }}
          token: ${{ env.TELEGRAM_TOKEN }}
          message: |
            ‼️<i>Maybe</i> <b>Pollens AI Image Models source is down</b>‼️
            Failed 3 times to fetch image model list.
          format: html
